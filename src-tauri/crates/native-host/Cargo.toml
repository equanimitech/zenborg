[package]
name = "zenborg-native-host"
version = "0.1.0"
edition = "2021"
description = "Chrome native messaging host — vault bridge for the browser extension."

[[bin]]
name = "zenborg-native-host"
path = "src/main.rs"

[dependencies]
serde_json = "1.0"
chrono = "0.4"
dirs = "5.0"
notify = "6.1"
observer-core = { path = "../observer-core" }
